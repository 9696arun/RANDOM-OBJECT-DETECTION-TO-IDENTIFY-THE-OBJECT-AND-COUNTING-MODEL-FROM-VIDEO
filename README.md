# RANDOM-OBJECT-DETECTION-TO-IDENTIFY-THE-OBJECT-AND-COUNTING-MODEL-FROM-VIDEO
A Random Object Detection and Counting Model from Video is a system that processes video frames to detect and identify objects using deep learning models (like Mobilenet or SSD), tracks them across frames using tracking algorithms (e.g., SORT), and counts them based on movement patterns. 
